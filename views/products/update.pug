extends ../../layouts/layout.pug

block content
  h1 Update

  form(action="/shops/products/update" method="POST")
    .form-group
      input(name="id" type="hidden" value=product.id)
    .form-group
      label(for='updateName') Name
      input#updateName.form-control(name="title" type="text" value=product.title)
    .form-group
      label(for='updateAuthor') Athour
      input#updateAuthor.form-control(name="description" type="text" value=product.description)

    button.btn.btn-primary(type='submit') Update
